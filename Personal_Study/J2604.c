#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#if 01
int main()
{
	char plate[51] = 0;
	int i = 0;
	int ans = 0;
	scanf("%s", plate);
	while (plate[i + 1] != 0)
	{
		if (plate[i] == '(')
		{

		}
	}
}
#endif